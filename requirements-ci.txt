# CI-only Python test dependencies.
# Installed by .github/workflows/ci.yml → test-python job → "Install base test deps".
# Kept separate from pyproject.toml because:
#   1. Production agents don't need pytest, pytest-cov, aiohttp, etc.
#   2. Pinning here lets dependabot bump them independently of app deps.
#   3. Reproducible CI: a hash-locked file is faster than resolving each time.

# Core test framework + plugins
pytest>=8.0,<9.0
pytest-asyncio>=1.4.0,<2.0
pytest-benchmark>=4.0,<5.0
pytest-cov>=5.0,<6.0

# Test-time utilities (HTTP + minimal web framework)
aiohttp>=3.9,<4.0
flask>=3.0,<4.0
