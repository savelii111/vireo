[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vireo-shared"
version = "0.1.0"
description = "Vireo — shared types and protocols for all agents."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["vireo_shared*"]
