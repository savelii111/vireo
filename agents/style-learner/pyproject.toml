[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vireo-style-learner"
version = "0.1.0"
description = "Vireo Style Learner — analyzes creator style and produces StyleDNA."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["vireo_style_learner*"]
